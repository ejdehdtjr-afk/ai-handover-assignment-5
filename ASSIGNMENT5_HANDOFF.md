# 과제 5 AI 작업 인계 문서

## 목표
달러 금액을 입력하면 과제 4의 현재 USD/KRW 환율로 원화 예상액이 즉시 바뀌게 한다.

완료 목표는 고정 검사 `T05-N01`부터 `T05-R03`까지 10개를 모두 통과하는 것이다.

## 현재 상태
AI A가 정상 계산 설계를 완료하고 검사 4/10 지점에서 계획대로 중단했다. AI B가 오류·접근성·모바일·회귀 처리를 완료했다.

- 과제 4 기준 버전: `78b0e4c`
- AI A 계획 중단 기준: `T05-N01`~`T05-N04`
- AI B 완료 기준: `T05-N01`~`T05-R03`

## 실행 방법
공개 GitHub Pages 주소를 열고 ‘달러 금액별 원화 환산기’에서 값을 바꾼다.

로컬 실행은 `pnpm install --frozen-lockfile` 후 `pnpm dev`를 사용한다. 정식 검증은 `pnpm build`로 수행한다.

## 통과한 검사
정상 `T05-N01`~`T05-N04`, 오류 `T05-E01`~`T05-E03`, 회귀 `T05-R01`~`T05-R03` — 총 10/10 통과.

각 검사의 입력과 기대 결과는 `ASSIGNMENT5_TEST_PLAN.md`에 고정되어 있다.

## 남은 문제
기능 문제는 없음. 실제 작업 세션의 시작·종료 시각과 호출 수는 `ASSIGNMENT5_EVIDENCE_LOG.md`에 확보했다. 서로 다른 두 AI의 서비스·모델 사용 여부와 4/10 중단 원본 커밋은 확인되지 않았다.

## 다음 행동
공개 주소에서 검증 안내서의 3단계 행동을 수행하고 `CHECKLIST_IMPLEMENTATION_STATUS.md`의 ‘확인 필요’ 두 항목을 원본 자료와 대조한다.

## 건드리면 안 되는 부분
과제 4의 환율 기록 2건, 장애 5종, 중복 방지, 변화 계산과 ECB 출처를 변경하지 않는다.

